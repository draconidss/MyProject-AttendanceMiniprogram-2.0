spring:
  application:
    name: attendance
  datasource:
    username: root
    password: 0.00.0
    url: jdbc:mysql://localhost:3306/attendance2
    driver-class-name: com.mysql.jdbc.Driver


server:
  port: 8885 #tomcat端口号

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.lifeisgg.attendance.dao: debug